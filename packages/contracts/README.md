# Shared contracts

OpenAPI at `/openapi.json` is the canonical Sprint 01 API contract. Generate typed clients from the running backend schema in a later change; do not manually diverge authorization semantics.
