# Security

Report vulnerabilities privately to the repository owner. Do not include passwords, refresh tokens, or production data in an issue. Rotate exposed secrets immediately. Production requires strong JWT and bootstrap secrets, HTTPS, explicit CORS origins, separate database users and isolated Redis.
