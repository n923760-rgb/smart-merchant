# Contributing

Open a branch and PR. Run backend lint/type checks and integration tests, web checks, and Flutter checks. Add migrations with forward and reverse paths, tests for tenant permissions and a decision record for architecture changes. Keep changes scoped.
