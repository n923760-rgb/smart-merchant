# ADR 0002 — Multi-tenancy

Status: accepted. One shared application/database with mandatory organization-scoped queries and composite foreign keys on branch-owned resources. A foreign UUID is treated as not found. Cross-tenant tests are mandatory.
