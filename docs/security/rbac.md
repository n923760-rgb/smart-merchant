# RBAC

`OWNER` has all Sprint 01 permissions. Branch-scoped `MANAGER` has read access only to its assigned branch. Other initial templates intentionally have minimal or zero grants until specific workflows are designed. The server checks permissions for every mutation, and web navigation checks actual API access. Revisit grant catalogs before any POS operation.
